pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
              sh '''#!/bin/bash
                    echo "I passed AWS"                  
                   '''
            }
        }
    }
}
